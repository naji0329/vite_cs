const detect_com = document.querySelector(".detect_com");
alert(detect_com)
const detect_dia = document.querySelector(".detect_dia")
detect_com.addEventListener('click', function() {
  alert('ss'),
  detect_dia.innerHTML = "ssss";
});